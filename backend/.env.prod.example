# ========================================
# PROD Environment Variables
# Study Helper Backend - Production Server
# Created: 2025-11-11
# Author: Juandbpimentel
# ========================================
# Este arquivo é usado para o ambiente de PRODUÇÃO.
# 
# COMO USAR:
# 1. Copie este arquivo: cp .env.prod.example .env.prod
# 2. Edite o arquivo .env.prod com as credenciais de PRODUÇÃO
# 3. Configure no servidor ou no serviço de deploy
# 
# ⚠️  ATENÇÃO MÁXIMA:
# - Use credenciais FORTES e ÚNICAS
# - NUNCA use as mesmas credenciais do DEV
# - NUNCA commite este arquivo com valores reais
# - Configure secrets diretamente no servidor/CI
# ========================================

# ========================================
# Application Configuration
# ========================================
SPRING_PROFILES_ACTIVE=prod

# ========================================
# Server Configuration
# ========================================
SERVER_PORT=8080

# ========================================
# Database Configuration - PostgreSQL
# ========================================
# ⚠️  IMPORTANTE: Use credenciais FORTES para produção!
# 
# Configuração para o banco de dados de PRODUÇÃO
# Recomendado: Supabase Pro, AWS RDS, ou outro serviço gerenciado
#
# Formato: jdbc:postgresql://HOST:PORT/DATABASE
# Exemplo Supabase: jdbc:postgresql://db.xxx.supabase.co:5432/postgres
SPRING_DATASOURCE_URL=jdbc:postgresql://your-prod-db-host.supabase.co:5432/postgres
SPRING_DATASOURCE_USERNAME=your_prod_username
SPRING_DATASOURCE_PASSWORD=your_VERY_STRONG_prod_password_123!@#

# ========================================
# Connection Pool Configuration
# ========================================
# Ajuste conforme o plano do seu banco de dados
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=10
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=5
SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT=20000
SPRING_DATASOURCE_HIKARI_IDLE_TIMEOUT=300000

# ========================================
# JWT Configuration
# ========================================
# ⚠️  CRÍTICO: Use um secret FORTE e ÚNICO para produção!
# Gere com: openssl rand -base64 32
# NUNCA use o mesmo secret do DEV!
JWT_SECRET=YOUR_SUPER_SECRET_JWT_KEY_MUST_BE_AT_LEAST_256_BITS_REPLACE_THIS_WITH_openssl_rand_base64_32
JWT_EXPIRATION=86400000

# ========================================
# CORS Configuration
# ========================================
# Adicione APENAS as URLs de produção do frontend
# Exemplo: URL do Netlify Production
ALLOWED_ORIGINS=https://studyhelper.com,https://www.studyhelper.com

# ========================================
# Security Configuration
# ========================================
# Configure SSL se estiver usando HTTPS direto no backend
# (Geralmente o serviço de deploy como Railway/Render já fornece SSL)
SSL_ENABLED=false

# ========================================
# Logging Configuration
# ========================================
# Em produção, use logs menos verbosos
LOGGING_LEVEL_ROOT=WARN
LOGGING_LEVEL_COM_STUDYHELPER=INFO
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_WEB=WARN

# ========================================
# Monitoring & Error Tracking
# ========================================
# Configure Sentry para tracking de erros em produção
# SENTRY_DSN=https://your-sentry-dsn-for-prod@sentry.io/project-id
# SENTRY_ENVIRONMENT=production
# SENTRY_TRACES_SAMPLE_RATE=0.1

# ========================================
# Performance & Caching (opcional)
# ========================================
# SPRING_CACHE_TYPE=redis
# SPRING_REDIS_HOST=your-redis-host
# SPRING_REDIS_PORT=6379
# SPRING_REDIS_PASSWORD=your-redis-password

# ========================================
# Email Configuration (opcional)
# ========================================
# SPRING_MAIL_HOST=smtp.gmail.com
# SPRING_MAIL_PORT=587
# SPRING_MAIL_USERNAME=your-email@gmail.com
# SPRING_MAIL_PASSWORD=your-app-password
# SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
# SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
