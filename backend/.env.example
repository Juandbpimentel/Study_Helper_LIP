# ========================================
# Environment Variables - Local Development
# Study Helper Backend
# ========================================
# COMO USAR:
# 1. Copie este arquivo: cp .env.example .env
# 2. Ajuste os valores se necessário
# 3. Execute: docker-compose up -d (para subir o banco)
# 4. Execute: ./gradlew bootRun
# ========================================

# Database Configuration (usado pelo Docker Compose)
POSTGRES_DB=studyhelper
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# PgAdmin Configuration (opcional - interface web)
PGADMIN_EMAIL=admin@studyhelper.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Application Configuration (opcional - só se precisar sobrescrever)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/studyhelper
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=postgres
# JWT_SECRET=your-custom-secret-key
# ALLOWED_ORIGINS=http://localhost:3000
