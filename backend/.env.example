# ========================================
# DEFAULT Environment Variables (Local Development)
# Study Helper Backend
# Created: 2025-11-11
# Author: Juandbpimentel
# ========================================
# Este arquivo é um exemplo de configuração para desenvolvimento local.
# 
# COMO USAR:
# 1. Copie este arquivo: cp .env.example .env
# 2. Edite o arquivo .env com suas credenciais locais
# 3. O arquivo .env está no .gitignore (não será commitado)
# 
# IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!
# ========================================

# ========================================
# Application Configuration
# ========================================
SPRING_PROFILES_ACTIVE=dev

# ========================================
# Server Configuration
# ========================================
SERVER_PORT=8080

# ========================================
# Database Configuration - PostgreSQL
# ========================================
# Para desenvolvimento local, você pode usar:
# - PostgreSQL local instalado
# - PostgreSQL via Docker (recomendado)
# - Supabase (para ambiente similar ao de produção)
#
# Formato: jdbc:postgresql://HOST:PORT/DATABASE
# Exemplos:
#   Local: jdbc:postgresql://localhost:5432/studyhelper
#   Supabase: jdbc:postgresql://db.xxx.supabase.co:5432/postgres
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/studyhelper
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# Variáveis extras para Docker Compose
# O docker-compose.yml usa estas variáveis para criar o container
SPRING_DATASOURCE_DB=studyhelper
SPRING_DATASOURCE_PORT=5432

# ========================================
# JWT Configuration
# ========================================
# Para desenvolvimento local, use qualquer valor
# Para produção, gere um valor seguro com: openssl rand -base64 32
JWT_SECRET=dev-secret-key-change-this-minimum-256-bits-for-local-development
JWT_EXPIRATION=86400000

# ========================================
# CORS Configuration
# ========================================
# Adicione as origens permitidas separadas por vírgula
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ========================================
# Logging (opcional)
# ========================================
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_STUDYHELPER=DEBUG

# ========================================
# PgAdmin Configuration (Docker)
# ========================================
# Configurações para acessar o PgAdmin via http://localhost:5050
PGADMIN_EMAIL=admin@studyhelper.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
POSTGRES_PORT=5432

# PgAdmin Configuration
PGADMIN_EMAIL=admin@studyhelper.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ========================================
# Como usar Docker Compose
# ========================================
# 1. Configure as variáveis acima
# 2. Execute: docker-compose up -d
# 3. O banco estará em: localhost:${POSTGRES_PORT}
# 4. PgAdmin em: http://localhost:${PGADMIN_PORT}
